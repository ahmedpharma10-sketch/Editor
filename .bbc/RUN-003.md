# Diffusion Studio BBC RUN-003

REQUEST_ID: `DIFFUSION-STUDIO-GITHUB-SETUP-20260905`
BBC_VERSION: `v3.0`
RUN_ID: `RUN-003`
STARTED_AT: `2026-09-05T13:19Z`
READY_WORK_AT_START: `YES`
SELECTED_UNITS: `DS-008, DS-009_PREP`
REHYDRATION_STATUS: `FULL_RELOAD_REQUIRED_AND_PASS`
AUTHORITY_CHANGE: current Personal OS routes BBC v3.0; complete mandatory BBC stack, Software Development, GitHub App Builder, and ChatGPT MD Builder authorities reloaded.
CTX_MICRO: `CLEAN`
CTX_PRESSURE: `NOT_EXPOSED`
CONTEXT_HEALTH: `HEALTHY`

## Reconciled physical state at run start

- Pull request #1 remains open and mergeable on isolated branch.
- Current branch head at run start: `f52828fe68fc10d38f28d67e791ba63e68ce1a6d`.
- Previously verified CI source checkpoint remains `45c78cb41aa068cb76f5e52ad56756d89a4b82ed`, run `33963597565`, artifact `diffusionstudio-web-dist`.
- Netlify now exposes a real production deploy for project `diffusion-studio-editor-ahmed`: deploy `6a9c1338108dd4d98cbebada`, state `READY`, production URL `https://diffusion-studio-editor-ahmed.netlify.app`, published `2026-09-05T13:04:19.199Z`, upload summary 19 files with 3 HTML pages and 16 assets. Netlify metadata does not bind the deploy to a Git commit, so this is `DEPLOYED_UNBOUND_PENDING_VERIFICATION`, not `RUNTIME_VERIFIED`.
- The CI artifact ZIP was independently materialized and inspected. It contains 24 web files including `index.html`, callback pages, fonts, waveform worker, CSS/JS chunks and image assets. Local ZIP SHA-256 is `9d735645db9b649fee1bcebe9b3d72f16cebd8a3a79ba3b6fd6966e1955a1b1`; this does not replace the server-side Actions artifact digest.
- Chat Browser Worker issue #20 was triggered against the Netlify URL, but Actions run `33967985550` failed before any workflow step (`steps=[]`). Job logs are unavailable through the API redirect. This is classified as a browser-worker execution-surface failure, not an application runtime failure.

## Strategy change

Because the shared Chat Browser Worker failed before browser execution and direct sandbox DNS is unavailable, use the Editor repository's own GitHub Actions runtime for a source-bound hosted verification gate. The gate will rebuild the exact branch source, compare every local `apps/web/dist` file against the live Netlify production URL, verify COOP/COEP headers, and run a pinned headless Chromium smoke against the live site. This is within the frozen P04 outcome and does not broaden product scope.

RESULT: `RUNNING`
MEANINGFUL_PROGRESS: `YES`
NEXT_ACTION: add the bounded hosted-runtime verification gate on the isolated branch, run it, diagnose direct logs, then reconcile evidence into `.bbc/BBC-STATE.md`.